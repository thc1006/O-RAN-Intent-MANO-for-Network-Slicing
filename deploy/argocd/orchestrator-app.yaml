apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: oran-orchestrator
  namespace: argocd
  labels:
    component: mano-orchestrator
spec:
  project: default
  source:
    repoURL: https://github.com/thc1006/O-RAN-Intent-MANO-for-Network-Slicing.git
    targetRevision: main
    path: deploy/k8s/orchestrator
  destination:
    server: https://kubernetes.default.svc
    namespace: oran-orchestrator
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
    - CreateNamespace=true
    - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m